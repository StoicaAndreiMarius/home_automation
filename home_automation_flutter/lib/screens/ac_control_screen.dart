import 'package:flutter/material.dart';

class ACControlScreen extends StatelessWidget {
  const ACControlScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('AC Control Screen'));
  }
}
